// OWNER: FE-A · Module: Hợp đồng
// Endpoints: GET /contracts?customer_code=&status= · POST /contracts · (chi tiết ở ContractDetail)
// Gợi ý: PageHeader, DataTable (onRowClick -> navigate `/contracts/${code}`), Modal + FormField tạo mới, StatusBadge
import PageHeader from "../components/PageHeader";
import { EmptyState } from "../components/Feedback";

export default function Contracts() {
  // TODO(FE-A): load /contracts -> DataTable (mã, KH, tiêu đề, giá trị, StatusBadge); nút tạo (Modal)
  return (
    <div>
      <PageHeader title="Hợp đồng" />
      <EmptyState text="TODO (FE-A): danh sách + tạo hợp đồng. Click dòng -> /contracts/:code" />
    </div>
  );
}
