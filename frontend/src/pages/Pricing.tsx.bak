// OWNER: FE-A · Module: Bảng giá
// Endpoints: GET /pricing/lists?customer_code= · POST /pricing/lists · POST /pricing/lists/{code}/versions
//            POST /pricing/versions/{id}/submit · GET /pricing/effective?customer_code=&service_code=&date=
// Gợi ý: danh sách bảng giá + versions (StatusBadge), Modal tạo version (bảng nhập items), nút submit.
import PageHeader from "../components/PageHeader";
import { EmptyState } from "../components/Feedback";

export default function Pricing() {
  // TODO(FE-A): load /pricing/lists -> hiển thị list + versions; tạo version + submit
  return (
    <div>
      <PageHeader title="Bảng giá" />
      <EmptyState text="TODO (FE-A): bảng giá + version + submit duyệt" />
    </div>
  );
}
